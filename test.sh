# listing all files form current directory
ls -l


# rename test.sh to test2.sh
mv test.sh test2.sh

# remove all files in the current directory
rm *

# go back one directory
cd ..

# display open ports
netstat -an

# display open ports and process names
netstat -an | grep LISTEN

# push to github repo
git add .
git commit -m "commit message"
git push

# pull from github repo
git pull

# change to different branch and push to github repo
git checkout branch_name


# shell command for listing all files in the current directory
ls -l

